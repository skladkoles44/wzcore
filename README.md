# Wheelzone Core — System Docs (Bot1 + Bot2 + SQLite)

This repository currently contains **documentation artifacts** for the production messaging pipeline (Bot1 + Bot2 + SQLite) as implemented and verified in the chat session.

## Documents
- `docs/System_Overview.md` — authoritative technical overview (facts only)
- `docs/diagrams/sequence.mmd` — Mermaid sequence diagram of E2E processing

## Mermaid
To render the diagram:
- open `docs/diagrams/sequence.mmd` in any Mermaid-compatible viewer/editor.

